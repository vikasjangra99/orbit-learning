const OrbitAuth=(()=>{const gate=document.createElement('section');gate.className='auth-gate';gate.setAttribute('aria-label','Orbit Learning account');gate.innerHTML='<div class="auth-layout"><div class="auth-brand brand"><span class="brand-mark">o</span><span>orbit<span class="brand-dot">.</span></span></div><div class="auth-copy"><p class="eyebrow">A LEARNING SPACE THAT ADAPTS</p><h1>Learn what matters <span>to you.</span></h1><p>Build knowledge at your pace, across the subjects and skills that belong in your life.</p></div><div class="auth-card"><h2 id="authTitle">Welcome back.</h2><p id="authSubtitle">Sign in to continue your learning journey.</p><div class="auth-tabs"><button class="auth-tab active" data-auth-mode="signin">Sign in</button><button class="auth-tab" data-auth-mode="signup">Create account</button></div><form class="auth-form" id="authForm"><label for="authEmail">EMAIL</label><input class="auth-input" id="authEmail" type="email" autocomplete="email" required><label for="authPassword">PASSWORD</label><input class="auth-input" id="authPassword" type="password" autocomplete="current-password" minlength="8" required><button class="primary-button auth-submit" id="authSubmit" type="submit">Sign in <span>↗</span></button></form><button class="forgot-button" id="forgotPassword" type="button">Forgot password?</button><div class="auth-divider">or</div><button class="google-button" id="googleAuth" type="button">Continue with Google</button><p class="auth-message" id="authMessage" role="status"></p><p class="auth-footnote">Your learning data belongs to your account and is protected by cloud database policies.</p></div></div><div class="cloud-status">● Cloud account required</div>';document.body.prepend(gate);let mode='signin';function message(text,success=false){const el=document.getElementById('authMessage');el.textContent=text;el.classList.toggle('success',success)}function setMode(next){mode=next;document.querySelectorAll('[data-auth-mode]').forEach(tab=>tab.classList.toggle('active',tab.dataset.authMode===mode));document.getElementById('authTitle').textContent=mode==='signin'?'Welcome back.':'Create your learning space.';document.getElementById('authSubtitle').textContent=mode==='signin'?'Sign in to continue your learning journey.':'Use your email to keep your learning progress across devices.';document.getElementById('authSubmit').innerHTML=mode==='signin'?'Sign in <span>↗</span>':'Create account <span>↗</span>';document.getElementById('authPassword').autocomplete=mode==='signin'?'current-password':'new-password';message('')}function show(){gate.classList.remove('hidden')}function hide(){gate.classList.add('hidden')}document.querySelectorAll('[data-auth-mode]').forEach(tab=>tab.addEventListener('click',()=>setMode(tab.dataset.authMode)));document.getElementById('authForm').addEventListener('submit',async event=>{event.preventDefault();if(!window.OrbitCloud.configured){message('Cloud configuration is unavailable.');return}const email=document.getElementById('authEmail').value.trim();const password=document.getElementById('authPassword').value;const result=mode==='signin'?await window.OrbitCloud.signIn(email,password):await window.OrbitCloud.signUp(email,password);if(result.error){message(result.error.message);return}if(mode==='signup'&&!result.data.session){message('Check your email to confirm your account.',true);return}hide();window.location.reload()});document.getElementById('forgotPassword').addEventListener('click',async()=>{const email=document.getElementById('authEmail').value.trim();if(!email){message('Enter your email first.');return}const result=await window.OrbitCloud.resetPassword(email);if(result.error)message(result.error.message);else message('Password reset email sent.',true)});document.getElementById('googleAuth').addEventListener('click',async()=>{if(!window.OrbitCloud.configured){message('Cloud configuration is unavailable.');return}const result=await window.OrbitCloud.signInWithGoogle();if(result.error)message(result.error.message)});return{show,hide,setMode}})();